package com.recoverai.entity.enums;

public enum FailureReason {
    INSUFFICIENT_FUNDS,
    CARD_EXPIRED,
    BANK_DECLINED,
    NETWORK_ERROR,
    LIMIT_EXCEEDED,
    AUTHENTICATION_FAILED,
    UNKNOWN
}
